function contarPalavras(array) {
    let num_palavras = 0;

    let array_palavras = array.split

    return array_final;
}

const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question("Escreva um texto: ", (resposta1) => {
    let num_palavras_array = contarPalavras(resposta1);
    console.log("O array resultante da união é: " + array_final + ".");
    rl.close();
});
